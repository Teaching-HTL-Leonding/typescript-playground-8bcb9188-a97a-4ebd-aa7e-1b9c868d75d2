function setup() {
  // <<< Add your code here.
  //     Only add code between the curly braces (`{}`).
  createCanvas(800, 800)

  noFill();
  strokeWeight(8);
  stroke("Blue");
  circle(100, 100, 130);

  stroke("Black");
  circle(250, 100, 130);

  stroke("red");
  circle(400, 100, 130);

  stroke("yellow");
  circle(170, 180, 130);

  stroke("green");
  circle(320, 180, 130);
}
